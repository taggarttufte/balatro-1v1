# Extracted game source + vendored references — NEVER commit (copyright)
_reference/

# Run artifacts (training / campaign outputs; large, regenerable)
runs/
checkpoints/
checkpoints_*/
logs_*/
*.pt
# Phase 3 W2: tournament run artifacts (.npz + JSONL per run directory)
tournament/runs/
# agent training runs + checkpoints (W1, Phase 3)
agent/runs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/
ray-env/

# Logs / bundles
*.log
*.bundle

# Agent workspace
.claude/

# Temp files
.pytest_cache/
.coverage
.ipynb_checkpoints/
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-*
