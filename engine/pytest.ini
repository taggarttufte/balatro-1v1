# Makes engine the pytest rootdir for any invocation that targets a path under it,
# so engine/conftest.py is always loaded and the fork's balatro_sim is importable
# regardless of the working directory (e.g. `python -m pytest engine/tests` from the
# repo root, or `pytest` from inside engine).
[pytest]
pythonpath = .
testpaths = tests
